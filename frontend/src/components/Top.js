import React, { Component } from 'react';
import { Button } from 'react-bootstrap';

class Top extends Component {
  render() {
    return (
      <div>
        <Button>Login</Button>
      </div>
    );
  }
}

export default App;
